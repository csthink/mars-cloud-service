<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mars.cloud</groupId>
        <artifactId>mars-cloud-dependencies</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>mars-cloud-gateway</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>mars-cloud-gateway</name>
    <description>mars-cloud 网关：系统外部请求的统一入口，路由到各业务服务（响应式栈）</description>

    <dependencies>
        <dependency><groupId>com.mars.cloud</groupId><artifactId>mars-cloud-security-spring-boot-starter</artifactId><scope>test</scope></dependency>
        <dependency><groupId>com.mars.cloud</groupId><artifactId>mars-cloud-security-test-support</artifactId><scope>test</scope></dependency>

        <!--
          响应式网关。Spring Cloud 2025.x 起 artifactId 带栈名后缀：
          webflux 是响应式栈，webmvc 是 Servlet 栈；旧名 spring-cloud-starter-gateway 已不再使用。
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
        </dependency>

        <!--
          lb:// 路由需要客户端负载均衡。nacos-discovery starter 对 spring-cloud-loadbalancer
          只是 optional 依赖，不会传递进来，必须在这里显式声明。
        -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-nacos-spring-boot-starter</artifactId>
        </dependency>

        <!--
          直接依赖 common 取统一响应信封与错误码契约。
          网关是响应式栈，不能引 Servlet 栈的 mvc starter，信封只能从 common 拿。
        -->
        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-common</artifactId>
        </dependency>

        <!--
          客户端负载均衡的实例缓存用 Caffeine。不加的话 Spring Cloud LoadBalancer 退回内置的简易缓存，
          并在启动日志里明确提示它只适合开发与测试。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>

        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!--
          可观测性：链路追踪与 OTLP 导出、Prometheus 指标、结构化日志与调用链的关联、
          管理端口约定与管理端点认证。Actuator 与指标注册表由它带入，本模块不单独声明。
        -->
        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-observability-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- 测试 JVM 由框架 BOM 配置为预加载 mockito-core，本依赖不可省略 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!--
          Boot 4 把 WebTestClient 的测试切片拆到独立 artifact，
          AutoConfigureWebTestClient 不再随 spring-boot-starter-test 提供，必须显式引入。
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <!-- Netty's macOS resolver requires a classifier matching the host architecture. -->
    <profiles>
        <profile>
            <id>macos-aarch64-dns</id>
            <activation><os><family>mac</family><arch>aarch64</arch></os></activation>
            <dependencies>
        <dependency><groupId>com.mars.cloud</groupId><artifactId>mars-cloud-security-spring-boot-starter</artifactId><scope>test</scope></dependency>
        <dependency><groupId>com.mars.cloud</groupId><artifactId>mars-cloud-security-test-support</artifactId><scope>test</scope></dependency>

                <dependency>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-resolver-dns-native-macos</artifactId>
                    <classifier>osx-aarch_64</classifier>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>UTF-8</encoding>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                            <version>${spring-boot.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
