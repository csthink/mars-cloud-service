<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.mars.cloud</groupId>
        <artifactId>mars-cloud-dependencies</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath/>
    </parent>

    <artifactId>mars-cloud-monitor</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>mars-cloud-monitor</name>
    <description>运行中实例的监控面板：经注册中心发现实例，读取各实例的管理端点，实例状态变化或被移除时通知</description>

    <dependencies>
        <!-- 监控面板服务端，自带界面与从注册中心发现实例的能力 -->
        <dependency>
            <groupId>de.codecentric</groupId>
            <artifactId>spring-boot-admin-starter-server</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <!-- 面板自己的表单登录；管理端点的认证链也按它提供的端点匹配器建立 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- 从注册中心发现被监控的实例；本部署物自己也注册进去 -->
        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-nacos-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-observability-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>mars-cloud-monitor</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <!--
              监控面板的版本必须跟随本项目的 Boot 基线：4.0.x 对应 Boot 4.0，
              4.1.x 依赖 Boot 4.1。这条规则让误升在构建期就失败，而不是等运行期。
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce</id>
                        <goals><goal>enforce</goal></goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[17,)</version>
                                </requireJavaVersion>
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>de.codecentric:*:[4.1,)</exclude>
                                    </excludes>
                                    <message>监控面板 4.1.x 依赖 Spring Boot 4.1，与本项目的 4.0 基线不同线</message>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!--
      面板用 Netty 的 HTTP 客户端读取各实例的管理端点。Netty 在 macOS 上要用本机的 DNS 解析库，
      缺少时每次启动打一条 ERROR 并退回通用解析方式；该库按处理器架构区分 classifier，只在对应的
      macOS 上引入，与网关相同。
    -->
    <profiles>
        <profile>
            <id>macos-aarch64-dns</id>
            <activation><os><family>mac</family><arch>aarch64</arch></os></activation>
            <dependencies>
                <dependency>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-resolver-dns-native-macos</artifactId>
                    <classifier>osx-aarch_64</classifier>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

</project>
